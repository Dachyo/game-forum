# Используется docker-compose.yml. Скопируйте в .env рядом с docker-compose.yml.
# На сервере этот файл создаётся один раз вручную и НЕ хранится в git.

DB_USER=forum
DB_PASSWORD=replace-with-a-strong-password
DB_NAME=forum
SESSION_SECRET=replace-with-a-long-random-string
